# ITER-057: Proactive task loop — «напоминает сам» (reference parity) {#root}

> Статус: PLAN — на OK основателя. Создано 2026-07-15.
> Методология copy-first: механика, каденции, промпт-паттерны и анти-надоедание
> скопированы из референсной desktop-архитектуры (изучена по её реальному коду,
> все цифры — из первоисточника), адаптация только под наш стек
> (OCR-текст + Groq/локальные LLM вместо скриншотов+Gemini; наш MWNotification-стек).

---

## 0. Главный факт из референса {#insight}

**У референса НЕТ таймерных напоминаний вообще** (grep подтвердил: ни одного
scheduled-триггера, ни snooze, ни re-remind). Их «напоминает сам» — это конвейер:

```
извлечь (тихо, staged) → пере-ранжировать (LLM, каждый час) →
ПРОДВИНУТЬ топ-1 в актив (≈5 активных AI-задач) → ОДНО уведомление →
клик = чат с контекстом «почему я это получил»
```

Анти-надоедание — слоями, не кулдаунами: whitelist приложений, ~90% ожидаемых
«задач нет», обязательный дедуп-поиск ПЕРЕД извлечением, удалённые юзером
задачи как негативные примеры, staged-буфер (ничего не летит в список напрямую),
продвижение по одной, уведомления **по умолчанию выключены**.

Мы копируем этот конвейер (057.1–057.4) и ДОБАВЛЯЕМ поверх наш таймерный слой
из готовой спеки ITER-043 (дедлайны/овердью/утренний дайджест) — потому что
основатель явно просил «напоминал», а у нас уже есть спека с ReminderEngine.

## 0.1 Что уже есть у нас / чего нет {#gaps}
| Референс | У нас | Вердикт |
|---|---|---|
| TaskAssistant (staged-извлечение с экрана) | RealtimeScreenReactor + ScreenExtractor → staged | ЕСТЬ (упростить промпт-правила → 057.5) |
| TaskPrioritizationService (почасовой ре-ранк) | нет | **057.2** |
| TaskPromotionService (промоушен+уведомление) | нет — staged тихо гниют и авто-скрываются через 7д | **057.1 — ядро** |
| Notification click-to-chat + provenance | клик открывает вкладку, без контекста | **057.4** |
| Reminder loop (дедлайны) | НЕТ у референса; у нас спека ITER-043 готова, кода ноль | **057.3** |
| FocusAssistant («ты отвлёкся») | ничего | 057.6 (потом, отдельно) |

---

## 1. User stories {#stories}
- **US1.** Как юзер, я хочу, чтобы найденные на экране/во встречах задачи
  **сами всплывали по одной**, когда у меня освобождается слот — а не лежали
  молча в Review candidates, пока я сам не зайду.
  *Приёмка:* завершил/удалил AI-задачу → в течение 5 мин топовая staged
  продвинута + одно уведомление «New task: …»; активных AI-задач держится ≈5.
- **US2.** Как юзер, я хочу, чтобы порядок staged-задач был **умным** (мои цели,
  сроки, реальная важность), а мусор тонул.
  *Приёмка:* каждый час LLM пере-ранжирует staged (≥2 шт); vague-задачи уходят вниз.
- **US3.** Как юзер, я хочу **напоминания по времени**: задача с дедлайном
  напоминает в срок, просроченные — в утреннем дайджесте.
  *Приёмка:* по ITER-043 (ReminderEngine: due-напоминание, overdue-дайджест 09:00).
- **US4.** Как юзер, кликнув по уведомлению, я хочу **спросить «почему»** — и
  чат должен знать, из какого окна/контекста задача взялась.
  *Приёмка:* клик по баннеру → чат, где уведомление = предыдущая реплика
  ассистента + provenance (app, окно, суть, счёт релевантности).
- **US5.** Как юзер, я не хочу спама.
  *Приёмка:* уведомления промоушена — opt-in тумблер (по умолчанию ВЫКЛ, как у
  референса); баннеры в очередь по одному; одна задача уведомляет ровно один раз.

## 2. Инварианты {#invariants}
- **I1 — Staged-буфер святой.** Ничего не летит в активные задачи напрямую;
  только через промоушен. (Уже так — не ломать.)
- **I2 — Одно уведомление на задачу.** Дедуп на этапе извлечения, не на этапе
  уведомления. Продвинутая задача больше никогда не нотифицирует.
- **I3 — Local-first LLM.** Ре-ранк и извлечение: локальная модель → Pro → BYOK
  (наша стандартная цепочка). Ре-ранк не жжёт квоту минут (это /advice-путь).
- **I4 — Defaults как у референса**: промоушен-уведомления OFF, extraction ON,
  target 5 активных AI-задач, safety-таймер 5 мин, ре-ранк 3600с.

## 3. Итерации {#plan}

### 057.1 — Promotion loop (ядро «само всплывает») — СДЕЛАНО 2026-07-16 {#i1}
- [x] `TaskPromotionService`: цель ≈5 активных **screen-sourced** задач
      (`screenContextId != nil && status == "committed"` — staged-кандидаты
      приходят только с экрана, поэтому и счёт слотов по экрану; диктовочные
      задачи живут отдельно и слоты не занимают). Триггеры: (а) старт — тихо,
      строго ПОСЛЕ ITER-007-миграции (она была не-one-time и демотировала
      промоутнутое каждый запуск — теперь под флагом, Codex); (б) любая
      мутация задачи через MutationService-хуки (dismiss/delete/save — chat-
      dismiss приходит как .taskSaved) → слот мог освободиться; (в) safety-
      таймер 300с. Порядок v1 — свежесть (createdAt DESC) в пределах
      hygiene-окна 7д; relevanceScore появится вместе с ре-ранкером (057.2).
      Reentrancy-guard от само-поджига через хуки.
- [x] Уведомление: kind .task, "Task" / "New task: {…}", только при
      `taskPromotionNotificationsEnabled` (default **false**), тумблер в
      Settings → Second Brain → Screen Intelligence. Клик → Workspace.
- [x] TaskHygiene: кандидаты старше 7д не промоутятся (единая логика).
- [x] Тесты: слот-математика, newest-first, идемпотентность, stale-скип,
      dismissed-не-воскресают, master-toggle. 7/7 зелёные.

### 057.2 — Re-ranking (умный порядок) {#i2}
- [ ] `TaskPrioritizationService`: цикл-проверка каждые 300с (startup delay 90с);
      полный ре-ранк когда прошло ≥3600с И staged ≥2. Промпт по референсу:
      «выяви MISRANKED; критерии: цели юзера, срочность (dueAt), actionability,
      реальная важность; большинство AI-задач — шум, топи vague вниз»;
      JSON {reranked:[{id,new_position}]} → перезапись relevanceScore.
- [ ] Контекст промпта: цели (Goal-модель уже есть), топ-30 staged, 10 последних
      завершённых (позитив) + 10 dismissed (негатив).
- [ ] Тесты: JSON-парс ре-ранка, применение позиций, guard-ы (0/1 staged → skip).

### 057.3 — Reminder loop по времени (наша ITER-043, код с нуля) {#i3}
- [ ] Due-date capture fix: TaskExtractor/ScreenExtractor/RealtimeScreenReactor
      сегодня выбрасывают прошедшие/мягкие даты — сейчас dueAt есть только у ~4%
      задач, напоминать не о чем. Чинить по ITER-043.
- [ ] `ReminderEngine` (pure) + `ReminderScanService`: скан раз в минуту;
      due-напоминание в срок; overdue — в утренний дайджест 09:00 (не по одному).
- [ ] MWNotification kind .reminder + отметка «напомнено» на TaskItem
      (не ре-нотифицировать).
- [ ] Тесты: движок на чистых датах (due сейчас/скоро/прошёл/напомнено),
      дайджест-группировка.

### 057.4 — Click-to-chat + provenance {#i4}
- [ ] Клик по баннеру промоушена/инсайта → открыть MetaChat с уведомлением как
      предыдущей репликой ассистента + `<notification_context>`-блок
      (source_app, window, context_summary, relevance, откуда задача) — промпт
      «на вопрос “почему я это получил” отвечай из этого блока, не гадай».
      Окно переиспользования 60с (после — обычный клик в Tasks).
- [ ] Тесты: сборка provenance-блока (pure), маршрутизация клика.

### 057.5 — Extraction hardening по референс-промпту {#i5}
- [ ] RealtimeScreenReactor: с blacklist на **whitelist** (мессенджеры: Telegram,
      WhatsApp, Messages, Slack, Discord, zoom + браузеры только с keyword-титулами:
      Gmail, Jira, GitHub, todo, inbox…). Референс-инсайт: задачи живут в переписке.
- [ ] Промпт-правила: «коммитмент или неотвеченный запрос К юзеру»; forgettability-check
      («забудет ли юзер после переключения окна? нет → скип»); скип терминалов/IDE/
      таск-трекеров («уже трекается»); ~90% ожидаемо «нет задачи»; confidence ≥0.75;
      заголовок 6–15 слов с именем/проектом (hard-валидация уже частично есть).
- [ ] Дедуп: обязательный поиск похожих ПЕРЕД вставкой (embedding-близость >0.8 к
      активной/staged → reject; наш EmbeddingService уже умеет).
- [ ] Тесты: whitelist-гейты, title-валидация, дедуп-порог.

### 057.6 — Focus coach (отдельный трек, после 1–5) {#i6}
«Ты отвлёкся — вернись»: классификация focused/distracted по активному окну,
glow-рамка, кулдаун 10 мин, refocus-уведомление. Большой UX — выделим в
собственную итерацию после ядра.

## 4. Ключевые числа (все — из кода референса) {#numbers}
promotion target **5** · safety-таймер **300с** · ре-ранк **3600с** (check 300с,
startup 90с, мин. staged **2**) · extraction fallback **600с** · confidence
**0.75** · дедуп-similarity **>0.8** · title **6–15 слов** · баннер **6с** (уже
так у нас) · click-to-chat окно **60с** · промоушен-уведомления default **OFF**.

## 5. Corner cases {#corners}
Staged пуст → промоушен no-op тихо · юзер удалил продвинутую задачу → слот
освободился, НО эта же задача не возвращается (dismissed = негативный пример) ·
ре-ранк упал/LLM недоступна → порядок прежний, не блокирует промоушен · два
триггера промоушена одновременно (старт+таймер) → сериализация, не двойное
уведомление · оффлайн → всё локально работает (кроме cloud-ре-ранка — скип) ·
DND/away → баннер в очередь (не дропать; UN-канал — в 057.4, если успеем).

## 6. Definition of Done {#dod}
1. Сценарий целиком: экранная задача → staged → (час) ре-ранк → юзер закрыл
   активную → топ продвинулся + одно уведомление → клик → чат знает «почему».
2. Задача с dueAt напоминает в срок; просроченные — одним утренним дайджестом.
3. Ноль повторных уведомлений об одной задаче (тест).
4. Все дефолты по I4; тумблеры в Settings → Second Brain.
5. `swift test` зелёный; Codex-ревью пройдено; смок на машине основателя.

## Changelog
- [2026-07-15] Первичный план по deep-read референсного ProactiveAssistants-кода
  (координатор, task/insight/memory/focus, promotion/prioritization, notification
  UX — все каденции и промпт-правила из первоисточника) + gap-карта против нашей
  кодовой базы. Ключевое открытие: у референса нет таймерных напоминаний —
  «напоминание» = promotion loop; таймерный слой добавляем из нашей ITER-043.
