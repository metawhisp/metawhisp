# Шаг 4 — «Два окна одного приложения — это два окна»

**Дата:** 2026-08-25
**Статус:** в работе
**Источник требования:** ITER-065 §3 (фокусное окно как идентичность), ревью Codex по 071.2 (P1 #5)

---

## 1. Проблема человеческим языком

Приложение определяет, «то же самое окно передо мной или другое», **по заголовку окна**.
Заголовок пишет само приложение или веб-страница. Из этого следуют две беды:

**Беда А — два окна слипаются в одно.**
Открыты два окна Safari, оба называются «Inbox». Для MetaWhisp это одно и то же окно.
Текст из одной почты приписывается визиту другой. Дальше по цепочке: факт из одного
письма привязывается к разговору из другого.

**Беда Б — одно окно разваливается на два.**
Ты сидишь в одном окне, страница меняет свой заголовок (счётчик непрочитанных,
таймер). Нормализатор снимает спиннеры и часы, но не всё.

> **Исправлено после ревью:** беду Б рамкой закрывать НЕЛЬЗЯ. Если позволить
> геометрии перебивать заголовок, то браузер, сменивший «Inbox» на «Docs» не
> двигаясь, читается как то же неизменное окно — и больше никогда не попадает под
> захват: старый текст остаётся «текущим» вечно. Заголовок, переживший
> нормализатор, — это другой документ, и его надо смотреть. Косметическую
> болтанку заголовка чистит нормализатор, это его работа, не рамки.
>
> Итог: рамка **уточняет** правило заголовка, а не заменяет его.

## 2. Почему так вышло

У системы есть надёжный идентификатор окна — `windowID` из ScreenCaptureKit.
Мы его **уже получаем** (шаг 1 проводки визитов: `ScreenContextSnapshot.windowID`),
записываем в строку визита как справочные данные — но **не используем при сравнении**.

Причина запрета была настоящая: `ContextVisitCoordinator.isSameWindow` сравнивает
`windowID`, только если он есть у ОБЕИХ сторон. Момент решения «захватывать ли»
наступает ДО скриншота, а `windowID` появляется только внутри скриншота. Значит на
входе всегда `nil`, а после захвата — число. Асимметрия `nil` против числа = «другое
окно» = новый визит на каждом тике = шторм захвата. Поэтому действовало правило
**populate-nowhere**: не передавать `windowID` нигде.

## 3. Решение

> **Исправлено 2026-08-26 после ревью Codex.** Ниже был описан план, который не
> отгружен: взять `windowID` из Accessibility до скриншота. В продакшене оба
> места вызова передают `windowID: nil`, а разделяет одноимённые окна **рамка**,
> а не идентификатор. Раздел переписан под то, что реально работает; прежний
> текст обещал несуществующее поведение и несуществующие тесты.

Разделять одноимённые окна **рамкой окна**, прочитанной из Accessibility
**до** скриншота — тем же вызовом, что и заголовок (`focusedWindowIdentity`
читает и то и другое за один заход, иначе фокус успевает уйти между двумя
чтениями и имя окна A склеивается с геометрией окна B).

Рамка симметрична: она есть у обеих сторон сравнения или нет ни у одной, —
поэтому асимметрии `nil` против числа, которая делала `windowID` непригодным,
здесь не возникает.

`windowID` остаётся в модели как справочные данные и **в сравнении не
участвует**. Путь «взять его из AX до захвата» не закрыт, но и не начат.

### Правило сравнения (после шага)

Правило после ревью — рамка **добавляется** к заголовку, никогда его не отменяет:

```
то же окно  ⟺  то же приложение
            И  тот же нормализованный заголовок     ← сигнал смены документа
            И  (рамок нет ИЛИ рамки перекрываются)  ← разделение одноимённых окон
```

| Ситуация | Решение | Зачем |
|---|---|---|
| Заголовок изменился | другое окно → захват | Иначе смена вкладки невидима навсегда |
| Заголовок тот же, рамки далеко | другое окно → захват | Беда А: два «Inbox» рядом |
| Заголовок тот же, рамки рядом | то же окно, тихо | Обычное сидение в окне |
| Рамка есть только у одной стороны | то же окно | Появление/пропажа рамки — не смена окна (шторм) |

Плюс: на тихом тике базовая рамка **двигается за окном**. Иначе медленное
перетаскивание сравнивается со всё более старой позицией и однажды даёт ложный
новый визит из ничего.

## 4. User stories

**US-4-1.** Как пользователь, я хочу, чтобы два окна одного приложения с одинаковым
заголовком считались разными, — чтобы текст из одного не приписывался другому.
*Приёмка:* два окна с равными заголовками и разными системными ID дают два визита.

**US-4-2.** Как пользователь, я хочу, чтобы окно, само сменившее заголовок, оставалось
одним заходом, — чтобы время работы не дробилось на куски.
*Приёмка:* один системный ID + изменившийся заголовок = один визит, поколение +1.

**US-4-3.** Как пользователь, я не хочу, чтобы приложение начало сканировать экран
каждые 30 секунд из-за этой правки.
*Приёмка:* два тика подряд на неизменном окне дают ровно один захват; тест на
пропажу/появление ID.

## 5. Definition of Done

- [x] Рамка окна берётся из Accessibility до захвата, одним вызовом с заголовком
- [x] Обе стороны сравнения получают рамку симметрично; правило из таблицы §3
      реализовано (`ContextVisitCoordinator.isSameWindow`)
- [x] Тесты: два окна/один заголовок; переименование; сдвиг; рамка только у одной
      стороны; обе без рамки (старое поведение)
- [x] Гейт `scripts/regression.sh` — PASS, 1211 тестов (пол 1181), 2026-08-26
- [x] Живая проверка на базе: 6 ложных срабатываний идентичности на 1357 визитов —
      `specs/screen-agent/baselines/VISIT-EVIDENCE-2026-08-26.md`
- [x] Ревью Codex — прошло, замечания по этому шагу учтены в §3

**Не сделано и вынесено отдельно:**
- [ ] `windowID` из Accessibility как участник сравнения (сейчас справочное поле)
- [ ] Счётчик активационных захватов — сколько захватов приходится не на опрос,
      сейчас не измеримо (см. §1 файла доказательств)

## 6. Риски

| Риск | Что делаю |
|---|---|
| Шторм захвата из-за асимметрии | Правило «пропажа ID ≠ другое окно» + тест на обе стороны |
| AX отдаёт ID не для всех приложений | Фолбэк на заголовок; это ровно текущее поведение |
| ID переиспользуется системой после закрытия окна | Визит и так истекает по 5-минутному разрыву; ID сравнивается только внутри живого визита |
